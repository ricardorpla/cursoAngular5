import { GlosarioDirective } from './glosario.directive';

describe('GlosarioDirective', () => {
  it('should create an instance', () => {
    const directive = new GlosarioDirective();
    expect(directive).toBeTruthy();
  });
});
